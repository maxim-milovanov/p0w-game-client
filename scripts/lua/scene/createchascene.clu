-- 创建登录场景
nCreateScene = SN_CreateScene( enumCreateChaScene, "创建人物", "", FORM_CREATE_CHA, 300, 200, 100, 100 )	

SN_SetIsShowMinimap( FALSE )
SN_SetIsShow3DCursor( FALSE )

-- 场景UI


GP_GotoScene( nCreateScene )	-- GotoScene放在最后,因为涉及切换模板时的表单初始化